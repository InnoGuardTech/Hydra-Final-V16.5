version: "3.9"
services:
  hydra:
    build:
      context: .
      target: runtime
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      PORT: 8080
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./sessions:/app/sessions
