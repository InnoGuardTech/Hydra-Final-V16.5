# Legacy compatibility file.
# Source of truth is Poetry: pyproject.toml + poetry.lock
# To regenerate a pinned requirements export:
#   poetry export -f requirements.txt --output requirements.txt --without-hashes
